# hbo — project instructions

HBO Max → Serbian learning catalogue. Self-contained web UI (vanilla JS, no deps), live at https://den.tanaev.su/hbo/.

## Git / backup — MANDATORY

This project is backed up to **https://github.com/deetan/hbo** (public).
After any meaningful change: `git add -A && git commit && git push`. Do not leave work unpushed at the end of a session.
